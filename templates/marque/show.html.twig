{% extends 'base.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>NomMarque</th>
                <td>{{ marque.nomMarque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('marque_index') }}" class="btn btn-secondary">Retour</a>

    <a href="{{ path('marque_edit', {'id': marque.id}) }}" class="btn btn-sucess">Modifier</a>

    {{ include('marque/_delete_form.html.twig') }}
{% endblock %}
