{% extends 'base.html.twig' %}

{% block body %}
    <title>Edit Booking</title>
    <h1>Edit Booking</h1>

    {{ include('booking/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('booking_index') }}" class="btn btn-secondary">Retour</a>

    {{ include('booking/_delete_form.html.twig') }}
{% endblock %}
