{% extends 'base.html.twig' %}

{% block title %}Energie{% endblock %}

{% block body %}
    <h1>Energie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ energie.id }}</td>
            </tr>
            <tr>
                <th>NomEnergie</th>
                <td>{{ energie.nomEnergie }}</td>
            </tr>
            <tr>
                <th>TarifEnergie</th>
                <td>{{ energie.tarifEnergie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('energie_index') }}" class="btn btn-secondary">Retour</a>

    <a href="{{ path('energie_edit', {'id': energie.id}) }}" class="btn btn-sucess">Modifier</a>

    {{ include('energie/_delete_form.html.twig') }}
{% endblock %}
